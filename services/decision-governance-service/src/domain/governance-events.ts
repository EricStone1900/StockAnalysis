import { randomUUID } from 'node:crypto';
import type { ApprovalRecord, RiskReviewLink, TradeProposal } from './proposal.js';

export interface GovernanceEvent { readonly eventId: string; readonly subject: string; readonly schemaVersion: number; readonly occurredAt: string; readonly availableAt: string; readonly producer: string; readonly correlationId: string; readonly payload: Record<string, unknown>; }
const envelope = (subject: string, proposal: TradeProposal, payload: Record<string, unknown>, correlationId: string, occurredAt: string): GovernanceEvent => ({ eventId: randomUUID(), subject, schemaVersion: 1, occurredAt, availableAt: occurredAt, producer: 'decision-governance-service', correlationId, payload: { proposalId: proposal.proposalId, proposalVersion: proposal.proposalVersion, ...payload } });
export const proposalCreatedEvent = (proposal: TradeProposal, correlationId: string, occurredAt: string): GovernanceEvent => envelope('stock.decision-governance.trade-proposal.created.v1', proposal, { state: proposal.state, contentHash: proposal.contentHash }, correlationId, occurredAt);
export const riskReviewedEvent = (proposal: TradeProposal, review: RiskReviewLink, correlationId: string, occurredAt: string): GovernanceEvent => envelope('stock.decision-governance.risk-review.completed.v1', proposal, { evaluationId: review.evaluationId, policyVersion: review.policyVersion, verdict: review.verdict }, correlationId, occurredAt);
export const approvalDecidedEvent = (proposal: TradeProposal, approval: ApprovalRecord, correlationId: string, occurredAt: string): GovernanceEvent => envelope('stock.decision-governance.approval.decided.v1', proposal, { decision: approval.decision, actorId: approval.actorId, reason: approval.reason }, correlationId, occurredAt);
