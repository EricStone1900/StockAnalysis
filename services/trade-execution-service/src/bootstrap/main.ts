import { BadRequestException, Body, Controller, Get, Module, Param, Post } from '@nestjs/common'; import { NestFactory } from '@nestjs/core'; import { FastifyAdapter } from '@nestjs/platform-fastify'; import { readServiceConfig } from '@stock/config'; import { log } from '@stock/observability'; import { ExecutionService } from '../application/execution-service.js'; import type { ApprovedExecutionCommand, OrderIntentStatus } from '../domain/execution.js';
const serviceName = 'trade-execution-service';
@Controller() class HealthController { @Get('/live') live() { return { status: 'UP' }; } @Get('/ready') ready() { return { status: 'UP', dependencies: {} }; } @Get('/metrics') metrics() { return ''; } @Get('/version') version() { return { service: serviceName, version: '0.1.0' }; } }
@Controller('/api/v1/execution') export class ExecutionController { public constructor(private readonly service = new ExecutionService()) {} @Post('batches') createBatch(@Body() body: ApprovedExecutionCommand) { try { return this.service.createBatch(body); } catch (error) { throw new BadRequestException(error instanceof Error ? error.message : 'invalid execution command'); } } @Post('batches/:batchId/intents/:intentId/status') transition(@Param('batchId') batchId: string, @Param('intentId') intentId: string, @Body() body: { status: OrderIntentStatus }) { try { return this.service.transitionIntent(batchId, intentId, body.status); } catch (error) { throw new BadRequestException(error instanceof Error ? error.message : 'invalid intent transition'); } } }
@Module({ controllers: [HealthController, ExecutionController] }) class AppModule {} async function bootstrap() { const config = readServiceConfig({ ...process.env, SERVICE_NAME: serviceName }); const app = await NestFactory.create(AppModule, new FastifyAdapter()); await app.listen(config.PORT, '0.0.0.0'); log('service.started', { service: serviceName }); } if (process.env.NODE_ENV !== 'test') void bootstrap();
