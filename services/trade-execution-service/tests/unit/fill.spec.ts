import { createHash } from 'node:crypto'; import { describe, expect, it } from 'vitest'; import { ExecutionAggregate } from '../../src/domain/execution.js';
describe('成交回报', () => { it('支持部分成交、完全成交和重复回报幂等', () => { const aggregate = new ExecutionAggregate(); const base = { rebalanceBatchId: 'b-fill', decisionId: 'd', proposalVersion: 1, approvalId: 'a', riskEvaluationId: 'r', budgetReservationId: 'x', targetPortfolioVersion: 1, validUntil: '2099-01-01T00:00:00Z', legs: [{ legId: 'l', securityId: 'SSE:600000', side: 'BUY' as const, quantity: '10' }] }; const batch = aggregate.createApprovedBatch({ ...base, contentHash: createHash('sha256').update(JSON.stringify(base)).digest('hex'), idempotencyKey: 'k' }); aggregate.transitionIntent(batch.rebalanceBatchId, batch.intents[0]!.intentId, 'SUBMITTED_MANUALLY'); expect(aggregate.recordFill(batch.rebalanceBatchId, { fillId: 'f1', intentId: batch.intents[0]!.intentId, filledQuantity: '4', fillPrice: '10', occurredAt: '2026-09-04T00:00:00Z', idempotencyKey: 'fk1' }).status).toBe('PARTIALLY_FILLED'); expect(aggregate.recordFill(batch.rebalanceBatchId, { fillId: 'f2', intentId: batch.intents[0]!.intentId, filledQuantity: '10', fillPrice: '10', occurredAt: '2026-09-04T00:00:00Z', idempotencyKey: 'fk2' }).status).toBe('FILLED'); }); });
