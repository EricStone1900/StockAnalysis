import { describe, expect, it, vi } from 'vitest'; import { ExecutionOutboxRepository } from '../../src/infrastructure/execution-outbox-repository.js';
describe('ExecutionOutboxRepository', () => { it('幂等保存审计事件并支持发布确认', async () => { const query = vi.fn().mockResolvedValue({ rows: [] }); const repository = new ExecutionOutboxRepository({ query }); const event = { eventId: 'e', subject: 'stock.trade-execution.fill.recorded.v1', schemaVersion: 1, occurredAt: '2026-09-04T00:00:00Z', availableAt: '2026-09-04T00:00:00Z', producer: 'trade-execution-service', correlationId: 'c', payload: {} }; await repository.append(event, 'batch'); await repository.markPublished('e'); expect(query.mock.calls[0]?.[0]).toContain('ON CONFLICT (event_id) DO NOTHING'); expect(query.mock.calls[1]?.[0]).toContain('published_at = now()'); }); });
