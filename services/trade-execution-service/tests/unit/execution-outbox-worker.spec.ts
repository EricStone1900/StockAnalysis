import { describe, expect, it, vi } from 'vitest'; import { ExecutionOutboxWorker } from '../../src/application/outbox-worker.js';
describe('ExecutionOutboxWorker', () => { it('发布成功后确认，失败保留未确认', async () => { const event = { eventId: 'e', subject: 'stock.trade-execution.fill.recorded.v1', aggregateId: 'b', payload: {}, availableAt: '2026-09-04T00:00:00Z' }; const store = { claim: vi.fn().mockResolvedValue([event]), markPublished: vi.fn() }; const worker = new ExecutionOutboxWorker(store, { publish: vi.fn() }); await expect(worker.publishBatch()).resolves.toEqual({ claimed: 1, published: 1, failed: 0 }); expect(store.markPublished).toHaveBeenCalledWith('e'); const failed = new ExecutionOutboxWorker(store, { publish: vi.fn().mockRejectedValue(new Error('down')) }); await expect(failed.publishBatch()).resolves.toEqual({ claimed: 1, published: 0, failed: 1 }); }); });
