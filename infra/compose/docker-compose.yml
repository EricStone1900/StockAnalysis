name: stock-analysis-infra

services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD_FILE: /run/secrets/postgres_password
    secrets: [postgres_password]
    ports: ["127.0.0.1:5433:5432"]
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./postgres-init.sql:/docker-entrypoint-initdb.d/10-services.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 3s
      retries: 20

  temporal-postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: temporal
      POSTGRES_PASSWORD_FILE: /run/secrets/temporal_postgres_password
      POSTGRES_DB: temporal
    secrets: [temporal_postgres_password]
    volumes: [temporal-postgres-data:/var/lib/postgresql/data]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U temporal -d temporal"]
      interval: 5s
      timeout: 3s
      retries: 20

  temporal:
    image: temporalio/auto-setup:1.27.2
    depends_on:
      temporal-postgres: { condition: service_healthy }
    environment:
      DB: postgres12
      DB_PORT: 5432
      POSTGRES_SEEDS: temporal-postgres
      POSTGRES_USER: temporal
    secrets: [temporal_postgres_password]
    entrypoint:
      - /bin/sh
      - -ec
      - >-
        export POSTGRES_PWD="$(cat /run/secrets/temporal_postgres_password)";
        exec /etc/temporal/entrypoint.sh autosetup
    ports: ["127.0.0.1:7233:7233"]

  nats:
    image: nats:2.10-alpine
    command: ["-js", "-sd", "/data", "-m", "8222"]
    ports: ["127.0.0.1:4222:4222", "127.0.0.1:8222:8222"]
    volumes: [nats-data:/data]

  redis:
    image: redis:7.4-alpine
    command: ["redis-server", "--appendonly", "no"]
    ports: ["127.0.0.1:6379:6379"]
    volumes: [redis-data:/data]

  minio:
    image: minio/minio:RELEASE.2025-04-22T22-12-26Z
    command: server /data --console-address :9001
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD_FILE: /run/secrets/minio_root_password
    secrets: [minio_root_password]
    ports: ["127.0.0.1:9000:9000", "127.0.0.1:9001:9001"]
    volumes: [minio-data:/data]

  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.120.0
    command: ["--config=/etc/otelcol-contrib/config.yaml"]
    volumes: ["../otel/collector.yaml:/etc/otelcol-contrib/config.yaml:ro"]
    ports: ["127.0.0.1:4317:4317", "127.0.0.1:4318:4318"]

  market-data-service:
    build:
      context: ../..
      dockerfile: services/market-data-service/Dockerfile
    environment:
      MARKET_DATA_DATABASE_URL: postgresql://market_data:market_data_local_only@postgres:5432/market_data
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY_FILE: /run/secrets/minio_root_password
      ARTIFACT_BUCKET: artifacts
      NATS_URL: nats://nats:4222
      MARKET_DATA_IMPORT_TOKEN: ${MARKET_DATA_IMPORT_TOKEN:-}
      BAOSTOCK_MAX_ATTEMPTS: ${BAOSTOCK_MAX_ATTEMPTS:-3}
      BAOSTOCK_INITIAL_BACKOFF_SECONDS: ${BAOSTOCK_INITIAL_BACKOFF_SECONDS:-1}
      BAOSTOCK_MIN_INTERVAL_SECONDS: ${BAOSTOCK_MIN_INTERVAL_SECONDS:-0.2}
      BAOSTOCK_QUERY_TIMEOUT_SECONDS: ${BAOSTOCK_QUERY_TIMEOUT_SECONDS:-30}
    secrets: [minio_root_password]
    depends_on:
      postgres: { condition: service_healthy }
      minio: { condition: service_started }
      nats: { condition: service_started }
    ports: ["127.0.0.1:3000:3000"]
    healthcheck:
      test: ["CMD", "python", "-c", "from urllib.request import urlopen; urlopen('http://localhost:3000/live')"]
      interval: 5s
      timeout: 3s
      retries: 20

  quant-research-service:
    build:
      context: ../..
      dockerfile: services/quant-research-service/Dockerfile
    environment:
      QUANT_RESEARCH_DATABASE_URL: postgresql://quant_research:quant_research_local_only@postgres:5432/quant_research
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY_FILE: /run/secrets/minio_root_password
      ARTIFACT_BUCKET: artifacts
    secrets: [minio_root_password]
    depends_on:
      postgres: { condition: service_healthy }
      minio: { condition: service_started }
    ports: ["127.0.0.1:3001:3000"]
    healthcheck:
      test: ["CMD", "python", "-c", "from urllib.request import urlopen; urlopen('http://localhost:3000/live')"]
      interval: 5s
      timeout: 3s
      retries: 20

  portfolio-risk-service:
    build:
      context: ../..
      dockerfile: services/portfolio-risk-service/Dockerfile
    environment:
      PORTFOLIO_DATABASE_URL: postgresql://portfolio_risk:portfolio_risk_local_only@postgres:5432/portfolio_risk
      PORTFOLIO_INTERNAL_TOKEN: ${PORTFOLIO_INTERNAL_TOKEN:?set PORTFOLIO_INTERNAL_TOKEN before starting portfolio}
    depends_on:
      postgres: { condition: service_healthy }
    ports: ["127.0.0.1:3002:3000"]
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://localhost:3000/live').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 5s
      timeout: 3s
      retries: 20

  decision-governance-service:
    build:
      context: ../..
      dockerfile: services/decision-governance-service/Dockerfile
    environment:
      DECISION_GOVERNANCE_DATABASE_URL: ${DECISION_GOVERNANCE_DATABASE_URL:?set DECISION_GOVERNANCE_DATABASE_URL before starting governance}
      NATS_URL: nats://nats:4222
      PORTFOLIO_RISK_BASE_URL: http://portfolio-risk-service:3000
      PORTFOLIO_INTERNAL_TOKEN: ${PORTFOLIO_INTERNAL_TOKEN:?set PORTFOLIO_INTERNAL_TOKEN before starting governance}
      GOVERNANCE_INTERNAL_TOKEN: ${GOVERNANCE_INTERNAL_TOKEN:?set GOVERNANCE_INTERNAL_TOKEN before starting governance}
    depends_on:
      postgres: { condition: service_healthy }
      nats: { condition: service_started }
    ports: ["127.0.0.1:3003:3000"]
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://localhost:3000/live').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 5s
      timeout: 3s
      retries: 20

  trade-execution-service:
    build:
      context: ../..
      dockerfile: services/trade-execution-service/Dockerfile
    environment:
      TRADE_EXECUTION_DATABASE_URL: ${TRADE_EXECUTION_DATABASE_URL:?set TRADE_EXECUTION_DATABASE_URL before starting execution}
      EXECUTION_SERVICE_TOKEN: ${EXECUTION_SERVICE_TOKEN:-}
      GOVERNANCE_AUTHORIZATION_BASE_URL: http://decision-governance-service:3000
      GOVERNANCE_INTERNAL_TOKEN: ${GOVERNANCE_INTERNAL_TOKEN:?set GOVERNANCE_INTERNAL_TOKEN before starting execution}
      NATS_URL: nats://nats:4222
    depends_on:
      postgres: { condition: service_healthy }
      nats: { condition: service_started }
    ports: ["127.0.0.1:3004:3000"]
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://localhost:3000/live').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 5s
      timeout: 3s
      retries: 20

  news-intelligence-service:
    build:
      context: ../..
      dockerfile: services/news-intelligence-service/Dockerfile
    environment:
      NEWS_INTELLIGENCE_DATABASE_URL: ${NEWS_INTELLIGENCE_DATABASE_URL:?set NEWS_INTELLIGENCE_DATABASE_URL before starting news}
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY_FILE: /run/secrets/minio_root_password
      ARTIFACT_BUCKET: artifacts
    secrets: [minio_root_password]
    depends_on:
      postgres: { condition: service_healthy }
      minio: { condition: service_started }
    ports: ["127.0.0.1:3005:3000"]
    healthcheck:
      test: ["CMD", "python", "-c", "from urllib.request import urlopen; urlopen('http://localhost:3000/ready')"]
      interval: 5s
      timeout: 3s
      retries: 20

  market-monitor-service:
    build:
      context: ../..
      dockerfile: services/market-monitor-service/Dockerfile
    ports: ["127.0.0.1:3006:3000"]
    healthcheck:
      test: ["CMD", "python", "-c", "from urllib.request import urlopen; urlopen('http://localhost:3000/live')"]
      interval: 5s
      timeout: 3s
      retries: 20

  market-regime-service:
    build:
      context: ../..
      dockerfile: services/market-regime-service/Dockerfile
    ports: ["127.0.0.1:3007:3000"]
    healthcheck:
      test: ["CMD", "python", "-c", "from urllib.request import urlopen; urlopen('http://localhost:3000/live')"]
      interval: 5s
      timeout: 3s
      retries: 20

  agent-service:
    build:
      context: ../..
      dockerfile: services/agent-service/Dockerfile
    environment:
      AGENT_DATABASE_URL: postgresql://agent_runtime:agent_runtime_local_only@postgres:5432/agent_runtime
      AGENT_ID: ${AGENT_ID:-stock-analysis}
      AGENT_MODEL_PROFILE: ${AGENT_MODEL_PROFILE:-fake}
      AGENT_PROMPT_VERSION: ${AGENT_PROMPT_VERSION:-fake-prompt-v1}
      AGENT_TASK_QUEUE: ${AGENT_TASK_QUEUE:-agent-stock-analysis}
      AGENT_DURABLE_CONSUMER: ${AGENT_DURABLE_CONSUMER:-agent-stock-analysis-v1}
      AGENT_ALLOWED_TOOLS: ${AGENT_ALLOWED_TOOLS:-}
    depends_on:
      postgres: { condition: service_healthy }
    ports: ["127.0.0.1:3009:3000"]
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://localhost:3000/live').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 5s
      timeout: 3s
      retries: 20

  platform-api-service:
    profiles: [web]
    build:
      context: ../..
      dockerfile: services/platform-api-service/Dockerfile
    environment:
      MARKET_DATA_SERVICE_URL: http://market-data-service:3000
    ports: ["127.0.0.1:3008:3000"]
    depends_on:
      market-data-service: { condition: service_healthy }

  web:
    profiles: [web]
    build:
      context: ../..
      dockerfile: apps/web/Dockerfile
    environment:
      PLATFORM_API_URL: http://platform-api-service:3000
    ports: ["127.0.0.1:5173:5173"]
    depends_on:
      platform-api-service: { condition: service_started }

secrets:
  postgres_password: { file: ./secrets/postgres_password }
  temporal_postgres_password: { file: ./secrets/temporal_postgres_password }
  minio_root_password: { file: ./secrets/minio_root_password }

volumes:
  postgres-data:
  temporal-postgres-data:
  nats-data:
  redis-data:
  minio-data:
