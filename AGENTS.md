# 仓库指南

## 项目结构与模块组织

本仓库保存股票分析平台的架构设计与交付规划，当前尚未包含应用源码或自动化测试套件。

- `docs/architecture/`：系统级设计、服务边界及各服务规格；从 `docs/architecture/stock-analysis-agent-system-design.md` 开始了解整体架构。
- `docs/development-phases/`：按能力域划分的原始分阶段实施计划。
- `docs/development-roadmap-v2/`：当前的详细路线图。各编号阶段通常包含 `00-stage-design.md`、实施计划、`90-test-plan.md` 和 `99-acceptance.md`。

新增文档应放入最贴近的既有领域目录，并保留数字排序规则，例如 `03-new-capability.md`。

## 文档工作流

当前没有构建、本地运行、格式化或自动测试命令。提交前使用以下 Git 命令检查文档变更：

```sh
git diff --check       # 检查空白字符错误
git diff -- docs/      # 审阅文档改动
```

使用相对 Markdown 链接，并确认目标路径存在。未来加入实现代码时，应在本文档补充安装、检查、测试和运行命令。

## 阶段与服务开发要求

阶段总设计必须说明目标与非目标、前置输入契约、边界与禁止事项、顺序依赖、服务/数据库/事件所有权、降级策略、测试矩阵和验收门禁。子开发文档还应包含文件清单、顺序步骤、关键接口或 Schema、幂等/安全/观测要求、就近测试和完成检查。

微服务严格按 S0～S6 推进，不得跳过：先冻结边界和契约（S0），再创建可独立运行的骨架（S1），用含事务、幂等和测试的最小纵向切片贯通（S2），随后补齐领域能力、契约事件集成、生产强化和独立发布验收。跨服务只使用生成的 OpenAPI/AsyncAPI 类型；写服务使用 Outbox，消费者使用 Inbox 与 `eventId` 幂等。大对象仅传 Artifact 引用与 Hash。

## 写作风格与命名规范

所有新增或更新的项目文档均使用中文保存；代码标识符、命令、路径、标准文件名及业内通用术语可保留英文。Markdown 应简洁、可执行，使用清晰标题、短段落以及聚焦的列表或表格。

路线图文件采用两位数字前缀与小写短横线主题名，如 `02-model-gateway.md`。保留标准阶段文件名（`00-stage-design.md`、`90-test-plan.md`、`99-acceptance.md`），以便查找关联内容。

视主题说明契约、责任方、输入输出、失败处理和验收标准。不得在文档中写入密钥、凭证、供应商令牌或生产账户信息。

## 验证要求

调整路线图范围时，同步更新对应阶段的设计、测试计划和验收文档。核对术语、服务名、事件名和交叉链接是否与架构文档一致。在阶段测试计划中记录人工验证步骤及可衡量的预期结果。

每个阶段和独立服务都要有 `90-test-plan.md` 与 `99-acceptance.md`。测试覆盖 Domain、Application、数据库集成、API/事件契约、组件、阶段 E2E 及安全/恢复等非功能场景；金融功能额外覆盖 PIT、未来数据泄漏、时区、交易日、复权、成本和停牌。

统一使用约定命令：Node 项目为 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm test:integration`、`pnpm test:contract`、`pnpm test:e2e`；Python 项目为 `uv run ruff check .`、`uv run mypy src` 和对应的 `pytest` 分层命令。验收记录必须包含提交、镜像/迁移/契约版本、测试报告、风险、回滚方案与签署人。硬风控、时间语义、数据质量、幂等、权限或恢复失败只能判定 `FAIL`。

## 提交与拉取请求指南

仓库尚无提交历史，暂未形成既有提交规范。使用祈使语气、带范围的提交主题，例如 `docs(roadmap): 明确市场数据验收标准`；每个提交应聚焦且完整。

拉取请求应说明决策或变更内容、列出受影响的文档路径，并在可用时关联议题或阶段。注明跨服务或安全影响；仅在渲染后的图表或视觉布局有实质变化时附截图。
