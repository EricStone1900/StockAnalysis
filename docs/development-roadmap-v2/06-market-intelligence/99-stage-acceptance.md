# 阶段06验收

状态：PASS（用户已完成 Ubuntu 人工验收）

验收基线：`f43a7cd`

- [x] 三个服务分别完成自己的99验收。
- [x] SecurityId、时间、版本和证据契约一致。
- [x] 新闻Candidate、异常事件和Regime快照可独立发布。
- [x] Fake Agent读写契约通过，真实Agent尚未接入。
- [x] 重复、迟到、故障和事件恢复通过。
- [x] 无跨库写入和共享ORM。
- [x] 任何服务故障不会生成伪正常市场结论。
- [x] `FREE_TIERED_10_20_30`使用一次批量快照本地筛选，P0/P1/P2分别每10/20/30分钟评估，默认50支；没有逐股或逐层请求风暴。
- [x] 50支连续20个交易日有效窗口成功率不低于98%，且没有连续两轮未恢复失败；80支启用记录满足同等门槛。
- [x] 100支压力测试的P95窗口完成时间不超过120秒；超时、限流、陈旧、覆盖不足、字段漂移和来源切换均失败关闭并保留审计证据。
- [x] 免费源仅标注为研究/Shadow的`best effort`准实时能力；未取得合规生产供应商前，不作为自动交易的唯一行情事实。
- [x] 版本化MonitorPolicy、阈值、冷却和升级/降级规则可追溯；盘中动作不重新计算日频Alpha，第二批仍只允许风险减仓或执行修正。

## 当前实现证据

- 新闻服务、盯盘服务、市场状态服务均已建立独立 Python 骨架、FastAPI 健康接口和分层测试。
- 阶段06契约冒烟：`python scripts/stage06-contract-smoke.py` 已通过。
- 当前本机验证仅证明确定性 DTO 和边界逻辑可组合；尚未证明真实消息总线、跨服务数据库权限、容器运行、历史回放和容量门禁。
- Ubuntu 人工验收必须补充：三服务 Compose 启动、`/live`/`/ready` 检查、真实 PostgreSQL/MinIO 连通、NATS 事件重放、故障恢复、50/80/100 支容量测试及审计日志检查。

在上述人工验收完成并由用户确认前，阶段06保持未验收状态。

## 纵向交付依赖调整

依据[ADR-020](../../architecture/adr/ADR-020-execution-consistency-and-delivery-gates.md)，本阶段增强能力不阻塞M1只读分析与M2隔离人工闭环；本阶段自身S0～S6、数据与安全测试及签署要求不变。验收应验证未启用本阶段时依赖方明确降级，不能伪造新闻、市场状态或学习结果；生产启用仍需原有门禁。
